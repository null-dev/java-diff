<target name="init">
    <!-- Create the time stamp -->
    <tstamp/>
    <!-- Create the build directory structure used by compile -->
    <mkdir dir="dist"/>
    <jar destfile="dist/app.jar"
       basedir="src/"
  />
  </target>
